name: Test Action with Email Step

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: 23f2002512@ds.study.iitm.ac.in
        run: echo "Hello, world! This step includes my email address."
